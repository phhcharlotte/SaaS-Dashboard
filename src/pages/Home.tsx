import { Container, Typography } from "@mui/material";

const Home: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Home</Typography>
    </Container>
  );
};

export default Home;
